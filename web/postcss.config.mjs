const config = {
  theme: {
    extend: {
      fontFamily: {
        mono: ["var(--font-dmmono)", "monospace"],
      },
    },
  },
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
